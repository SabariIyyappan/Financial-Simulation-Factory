{
  "mcpServers": {
    "brightdata": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "https://mcp.brightdata.com/mcp?token=${BRIGHTDATA_MCP_TOKEN}"
      ]
    }
  }
}
